"use client";


import styles from './styles.module.css';

const Cadastro: React.FC = () => {


    return (
        <>
            <h1 className={` ${styles.error_message} ${styles.text_bold} `}>Retornaando cadastro</h1>
            <h1 className={` ${styles.error_message} ${styles.text_bold} `}>Retornaando cadastro</h1>
            <h1 className={` ${styles.error_message} ${styles.text_bold} `}>Retornaando cadastro</h1>
            <h1 className={` ${styles.error_message} ${styles.text_bold} `}>Retornaando cadastro</h1>
            <h1 className={` ${styles.error_message} ${styles.text_bold} `}>Retornaando cadastro</h1>
        </>
    );
};

export default Cadastro;
